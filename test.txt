# 1. Cập nhật hệ thống và cài đặt Google Chrome Stable chính thức cho Linux ảo
!sudo apt-get update
!sudo apt-get install -y wget curl unzip libxi6 libgconf-2-4 libnss3 xdg-utils ffmpeg
!wget https://dl.google.com/linux/direct/google-chrome-stable_current_amd64.deb
!sudo dpkg -i google-chrome-stable_current_amd64.deb
!sudo apt-get install -f -y  # Sửa lỗi nếu thiếu gói phụ thuộc hệ thống

# 2. Cài đặt các thư viện Python phiên bản mới nhất bằng pip
!pip install selenium webdriver-manager